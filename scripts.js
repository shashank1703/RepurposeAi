function navigateTo(page) {
    // Simulating navigation for this example
    alert(`Navigating to ${page} page...`);
    // In a real application, you would use something like:
    window.location.href = `${page}.html`;
}
